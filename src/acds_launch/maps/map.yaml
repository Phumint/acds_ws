image: map_small.pgm
resolution: 0.0178253119 #0.05  # meters per pixel (ADJUST based on your floor plan scale)
origin: [0.0, 0.0, 0.0]  # [x, y, yaw] Bottom-left corner in meters
negate: 0  # 0 = white is free, black is occupied
occupied_thresh: 0.8  # Threshold for obstacle detection (0-1)
free_thresh: 0.5  # Threshold for free space (0-1)
